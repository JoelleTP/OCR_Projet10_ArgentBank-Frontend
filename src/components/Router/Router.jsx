import { BrowserRouter, Routes, Route } from "react-router";

function Router() {
    return (
        <BrowserRouter>
            <Routes>
                
            </Routes>
        </BrowserRouter>
    )
}

export default Router